interface:
  display_name: "AppGen"
  short_description: "$appgen inicia ou retoma o fluxo principal para criar um app corporativo."
  default_prompt: "$appgen"

policy:
  allow_implicit_invocation: true
